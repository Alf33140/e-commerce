{% extends 'base.html.twig' %}

{% block title %}Subcategorie{% endblock %}

{% block body %}
    <h1>Sous-catégorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subcategorie.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subcategorie.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_subcategorie_index') }}" class="btn btn-outline-primary">back to list</a>

    <a href="{{ path('app_subcategorie_edit', {'id': subcategorie.id}) }}" class="btn btn-outline-primary">edit</a>

    {{ include('subcategorie/_delete_form.html.twig') }}
{% endblock %}
