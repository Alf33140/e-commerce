{% extends 'base.html.twig' %}

{% block title %}Produit{% endblock %}

{% block body %}
    <div class="container">
        <h1>Produits</h1>

        <table class="table">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ product.id }}</td>
                </tr>
                <tr>
                    <th>Name</th>
                    <td>{{ product.name }}</td>
                </tr>
                <tr>
                    <th>Description du produit</th>
                    <td>{{ product.description }}</td>
                </tr>
                <tr>
                    <th>Prix (en thunes)</th>
                    <td>{{ product.Price }}</td>
                </tr>
            </tbody>
        </table>

        <a href="{{ path('app_product_index') }}" class="btn btn-primary mt-2">Revenir à la liste</a>

        <a href="{{ path('app_product_edit', {'id': product.id}) }}"class="btn btn-secondary mt-2">Modifier</a>

        <a href="{{ path('app_product_stock_add_history', {'id': product.id}) }}" class="btn btn-secondary mt-2">Historique des ajouts de stock</a>

        {{ include('product/_delete_form.html.twig') }}
    </div>
{% endblock %}
