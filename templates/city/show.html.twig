{% extends 'base.html.twig' %}

{% block title %}Cost{% endblock %}

{% block body %}
    <h1>Cost</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cost.id }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ cost.city }}</td>
            </tr>
            <tr>
                <th>Cost</th>
                <td>{{ cost.cost }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_city_index') }}">back to list</a>

    <a href="{{ path('app_city_edit', {'id': cost.id}) }}">edit</a>

    {{ include('city/_delete_form.html.twig') }}
{% endblock %}
